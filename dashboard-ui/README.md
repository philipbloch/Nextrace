# Nextrace dashboard UI

React/Vite dashboard for Nextrace.

Run the UI against the local FastAPI dashboard API:

```bash
cd dashboard-ui
npm install
npm run dev
```

Build the UI served by FastAPI:

```bash
npm run build
```

The production build writes to `src/nextrace/dashboard/static`.
