# Nextrace quick site

React/Vite quick site for Nextrace.

Run locally:

```bash
cd quick-site
npm install
npm run dev -- --host 127.0.0.1 --port 8787
```

Then open `http://127.0.0.1:8787`.

Build for deploy:

```bash
npm run build
```
